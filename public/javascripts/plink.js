CloudFlare.define("prosperlinks", ["prosperlinks/config", "cloudflare/config", "cloudflare/user", "cloudflare/account", "cloudflare/jquery1.7"], function(_config, _cfconfig, _user, _account, jqP)
{
    console.debug(_config);
    console.debug(_cfconfig);
    console.debug(_user);
    console.debug(_account);
});
